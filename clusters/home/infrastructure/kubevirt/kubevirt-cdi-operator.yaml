apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cdi-operator
  namespace: flux-system
spec:
  targetNamespace: cdi
  interval: 10m
  chart:
    spec:
      chart: cdi-operator
      version: "1.65.0"
      sourceRef:
        kind: HelmRepository
        name: kubevirt-charts
        namespace: flux-system
