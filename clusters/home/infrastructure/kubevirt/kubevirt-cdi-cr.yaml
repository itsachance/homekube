apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cdi-cr
  namespace: flux-system
spec:
  targetNamespace: cdi
  interval: 10m
  dependsOn:
    - name: cdi-operator
  chart:
    spec:
      chart: cdi-cr
      version: "1.65.0"
      sourceRef:
        kind: HelmRepository
        name: kubevirt-charts
        namespace: flux-system
