apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: authelia-forwardauth
  namespace: authelia
  labels:
    app: authelia
spec:
  forwardAuth:
    address: http://authelia-svc.authelia.svc.cluster.local:9091/api/authz/forward-auth
    #    trustForwardHeader: true
    authResponseHeaders:
      - Remote-User
      - Remote-Groups
      - Remote-Email
      - Remote-Name
